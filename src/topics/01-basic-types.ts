const name:string = 'John Doe';
const age:number = 25;
const isDeveloper:boolean = true;

let hpPoints:number | string = 95;

hpPoints = 'FULL';

console.log({
    name, age, isDeveloper, hpPoints
});

export {};